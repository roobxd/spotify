export enum MainView {
    HOME = 'HOME',
    SEARCH = 'SEARCH',
    ARTIST = "ARTIST",
    ALBUM = "ALBUM",
    PLAYLIST = "PLAYLIST"
  }

export enum AdditionalView {
  NOW_PLAYING = "NOW_PlAYING"
}